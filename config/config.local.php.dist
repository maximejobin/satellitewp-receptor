<?php
/**
 * Local overrides — copy to config.local.php (gitignored) and adjust.
 * Only include the keys you want to change; arrays merge recursively.
 */

declare(strict_types=1);

return [
    // Dev only: accept payloads without signature.
    // 'allow_unsigned' => true,

    // Web UI Basic auth. Generate the hash with:
    //   php -r "echo password_hash('your-password', PASSWORD_DEFAULT), PHP_EOL;"
    // 'web' => [
    //     'user'      => 'satellitewp',
    //     'pass_hash' => '$2y$10$…',
    // ],

    // 'probes' => [
    //     'enabled' => ['http', 'dns', 'tls', 'rdap', 'pagespeed'],
    //     'timeout' => 15,
    // ],

    // PageSpeed Insights. A key is effectively required — the anonymous quota
    // returns HTTP 429 immediately. Create one (free, ~25k req/day) at:
    // https://console.cloud.google.com/apis/credentials (enable "PageSpeed Insights API")
    'pagespeed' => [
        'api_key' => 'YOUR_PSI_API_KEY',
        // 'strategy'   => 'both',      // 'mobile' | 'desktop' | 'both'
        // 'locale'     => 'fr',        // BCP-47: localizes titles and displayValue
        // 'categories' => ['performance', 'accessibility', 'best-practices', 'seo'],
        // 'min_score'  => 90,          // performance score below which status = warn
    ],

    // Optional integrations — left commented so a fresh copy stays quiet.
    // A placeholder key here would produce confusing 401s; with the key absent
    // the probes report a clean "not configured" and the pipeline carries on.

    // BlogVault v6 — vulnerabilities/malware/backup. Account → API Credentials.
    // 'blogvault' => [
    //     'api_key' => '…',
    // ],

    // Wordfence Intelligence v3 — second, independent vulnerability source.
    // Generate under Account → Integrations. Only used by `wordfence:refresh`
    // (suggested: daily cron) — never called during a site scan.
    // 'wordfence' => [
    //     'api_key' => '…',
    // ],

    // Google sign-in for the web UI. Create the client under
    // Google Cloud Console → APIs & Services → Credentials
    //   → Create credentials → OAuth client ID → Web application,
    // and register this exact redirect URI on it:
    //   https://<votre-domaine>/auth/callback
    // (for local dev: http://127.0.0.1:8080/auth/callback)
    //
    // Access is granted only to addresses listed in data/users.json — being
    // able to sign in with Google is not enough. The first address in that
    // file is the admin and manages the others from the UI.
    // 'auth' => [
    //     'google' => [
    //         'client_id'     => '…apps.googleusercontent.com',
    //         'client_secret' => '…',
    //         // 'redirect_uri' => 'https://votre-domaine/auth/callback',
    //     ],
    // ],
];
